# Dictionaries

YAML source-of-truth editorial dictionary data lives here before import into SQLite.

Useful helper:

- run `bash scripts/dictionary/audit-coverage.sh` to spot thin coverage patterns across countries and categories before expanding entries
