# Dictionary Scripts

Planned responsibilities:

- validate YAML structure
- verify taxonomy references
- stamp bundle versions
- prepare import payloads for SQLite
- audit editorial coverage skew across country/category files

Current helper scripts:

- `validate.sh`: placeholder entrypoint for future strict validation
- `audit-coverage.sh`: lightweight coverage audit for `time_context`, `weather`, and `seasonality` counts by country/category
